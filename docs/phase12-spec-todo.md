# Phase 12 仕様要決定リスト

ギミックエンジン・インベントリ・数字オブジェクトの実装（2026-06-10 時点）で表面化した、
実装を進める前に仕様を確定しておくべき項目の一覧。
各項目に現状の実装挙動と推奨案を記載する。決定後は `docs/world-creation.md` に反映し、
本ファイルの該当項目を消し込むこと。

**決定済み（world-creation.md に反映済み・2026-06-10）:**
- ルール定義のバリデーション層 → セクション 9.11（サーバー保存時拒否 + クライアントはルール単位無効化）
- 無限ループ判定のカウント単位 → セクション 9.10（直接アクションは対象外・2 次以降の連鎖累計 100 超で検知）
- 入室時同期スナップショットの不足分 → セクション 9.9（インベントリ・オブジェクト表示/種類・BGM を追加）
- インベントリ細部 → セクション 9.3 / 9.8（種別 ID 単位・「持つ」と「付与する」の 2 アクション・
  「持つ」は全員選択不可・「付与する」は全員可・失敗は無言）。人数変動イベントの入力プレイヤー =
  入退室したプレイヤー → セクション 9.5
- 人数変動イベントの発火順序 → セクション 9.5（退室者のステート削除・インベントリ返却の前に発火）
- 順位条件 → セクション 9.6（対象プレイヤーの値が全在室者中 X 位以内・同値は同順位）
- ゲーム内ゲーム拡張 6 件 → エリア退出イベント（9.5）/ タイマーの値比較条件（9.6）/
  数字オブジェクトのタイマー参照・カウントダウン（3.9）/ 範囲乱数の最大値に「現在人数」（9.7）/
  移動速度変更・頭上マーカーアクション（9.8）。バリデーション項目も 9.11 に追記
- ルールテンプレート → セクション 9.12 新設（エディタ UI で定型ルール一式を挿入・
  ランタイム新概念なし・空きステート/タイマー自動割り当て・初期 6 種）。
  UI 仕様は screens-and-modes.md 11.7.4 に追記
- 状態リセットのスコープ → セクション 9.8 に範囲表を追加（プレイヤー系 = ステート +
  インベントリ返却 + 速度 100% + マーカー消去 / ワールド = ワールドステート + BGM +
  オブジェクト表示・種類状態 / すべて = 全要素 + タイマー + オブジェクト位置初期化。
  ループ検知時 = 「すべて」と同一 → 9.10）
- 数字オブジェクト細部 → セクション 3.9（参加順は在室者の参加順で詰める・固定値 0〜999・
  参照先プレイヤー不在時は 0 表示。描画方式はメッシュ/アトラス UV いずれかを実装時に選定）
- テクスチャコスト判定 → セクション 4.3 / 3.6（**配置ベース**に変更: ギミックの表示状態は
  コスト・オブジェクト数に影響しない。非表示も対象。切り替え先は種別・保存バリアントとも
  指定可。初期非表示フラグは追加しない — ルーム開始時イベント + 非表示アクションで代替）

関連実装: `Assets/Scripts/World/Gimmick/`（GimmickEngine / GimmickStateManager /
GimmickTimerLogic / GimmickValueResolver / GimmickInventoryLogic / NumberObjectSyncLogic）

---

## 1. ネットワーク権威モデルの明文化（world-creation.md 9.1 / 9.4）

**背景**: 9.4 は「ルール評価はルームオーナーが行い、結果を全クライアントに送信」とあるが、
9.2 のタイマーイベントは「各クライアントがローカル監視して発火 → オーナーが権威的に確認」とあり、
クライアント側でもエンジンを動かすのか曖昧。

**決めること**:
- エンジン実行はオーナーのみか（非オーナーは `GimmickEffect` リストの適用のみ行うか）
- タイマーイベントの検知責務: オーナーのみの監視で十分か（9.2 の記述の整理）
- 範囲乱数の共有方法: オーナー評価 + エフェクト送信モデルなら、エフェクトに解決済みの値
  （例: `WorldStateChangedEffect.NewValue`）が入るため追加の乱数同期は不要 — これを明文化するか
- エフェクト送信の形式（Netcode for GameObjects の RPC / NetworkVariable のどちらで運ぶか）

**推奨**: オーナーのみ評価・エフェクト適用方式（現エンジン設計と一致）。乱数の個別同期は不要と明記。

---

## 決定の優先度

| 優先 | 項目 | 理由 |
|---|---|---|
| 高 | 1. 権威モデル | ネットワーク統合の実装前に必須。Netcode for GameObjects の設計と合わせて決定する |
