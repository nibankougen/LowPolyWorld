[package]
name = "paint-engine"
version = "0.1.0"
edition = "2021"

[lib]
name = "paint_engine"
crate-type = ["cdylib", "staticlib"]

[dependencies]
image = { version = "0.25", default-features = false, features = ["png"] }
